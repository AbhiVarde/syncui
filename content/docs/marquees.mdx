---
title: Marquees
description: Sync UI offers various marquee animation styles to add dynamic content to your user interface.
---

Sync UI provides several marquee animation styles to display dynamic, scrolling content with smooth animations.

#### Horizontal Marquee

A horizontally scrolling carousel with smooth infinite animation.

<CodePreview
  preview={<MarqueeVariants variant="horizontal" />}
  code={`
import React from "react";
import { Box, Card, Stack, Avatar, Typography, IconButton, useTheme } from "@mui/material";
import { RxTwitterLogo } from "react-icons/rx";

const componentTestimonials = [
  {
    name: "Emma Rodriguez",
    handle: "@emma_frontend",
    avatar: "https://images.unsplash.com/photo-1494790108377-be9c29b29330",
    text: "SyncUI + CSS animations = smooth and fast! 🚀",
  },
  // Add more testimonials like these
];

const HorizontalMarquee = () => {
  const theme = useTheme();

  return (
    <Box sx={{
      position: "relative",
      overflow: "hidden",
      py: 2,
      "&::before, &::after": {
        content: '""',
        position: "absolute",
        top: 0,
        bottom: 0,
        width: 80,
        zIndex: 2,
      },
      "&::before": {
        left: 0,
        background: \`linear-gradient(to right, \${theme.palette.background.default}, transparent)\`,
      },
      "&::after": {
        right: 0,
        background: \`linear-gradient(to left, \${theme.palette.background.default}, transparent)\`,
      }
    }}>
      <Box
        sx={{
          display: "flex",
          gap: theme.spacing(2),
          width: "max-content",
          animation: "scrollX 30s linear infinite",
          "@keyframes scrollX": {
            "0%": { transform: "translateX(0)" },
            "100%": { transform: "translateX(-50%)" },
          },
        }}
      >
        {[...componentTestimonials, ...componentTestimonials].map((tweet, i) => (
          <Card
            key={i}
            sx={{
              minWidth: 280,
              p: 2,
              borderRadius: "12px",
              border: \`1px solid \${theme.palette.divider}\`,
              bgcolor: "background.paper",
            }}
          >
            <Stack spacing={1.5}>
              <Stack direction="row" spacing={1.5} alignItems="center">
                <Avatar
                  src={tweet.avatar}
                  sx={{
                    width: 40,
                    height: 40,
                    border: \`2px solid \${theme.palette.background.paper}\`,
                    boxShadow: theme.shadows[1],
                  }}
                />
                <Box>
                  <Typography variant="subtitle1" fontWeight={600}>
                    {tweet.name}
                  </Typography>
                  <Typography variant="body2" color="text.secondary">
                    {tweet.handle}
                  </Typography>
                </Box>
                <IconButton size="small" sx={{ color: "#1DA1F2", ml: "auto" }}>
                  <RxTwitterLogo />
                </IconButton>
              </Stack>
              <Typography variant="body1" sx={{ lineHeight: 1.5 }}>
                {tweet.text}
              </Typography>
            </Stack>
          </Card>
        ))}
      </Box>
    </Box>
  );
};
`}
/>

---

#### Vertical Marquee

A vertically scrolling carousel using pure CSS keyframes.

<CodePreview
  preview={<MarqueeVariants variant="vertical" />}
  code={`
import React from "react";
import { Box, Card, Stack, Avatar, Typography, IconButton, useTheme } from "@mui/material";
import { RxTwitterLogo } from "react-icons/rx";

const componentTestimonials = [
  {
    name: "Emma Rodriguez",
    handle: "@emma_frontend",
    avatar: "https://images.unsplash.com/photo-1494790108377-be9c29b29330",
    text: "Perfect for vertical content feeds. ⚡️",
  },
  // Add more testimonials like these
];

const VerticalMarquee = () => {
  const theme = useTheme();

  return (
    <Box sx={{
      height: 500,
      position: "relative",
      overflow: "hidden",
      "&::before, &::after": {
        content: '""',
        position: "absolute",
        left: 0,
        right: 0,
        height: 80,
        zIndex: 2,
      },
      "&::before": {
        top: 0,
        background: \`linear-gradient(to bottom, \${theme.palette.background.default}, transparent)\`,
      },
      "&::after": {
        bottom: 0,
        background: \`linear-gradient(to top, \${theme.palette.background.default}, transparent)\`,
      }
    }}>
      <Box
        sx={{
          display: "flex",
          flexDirection: "column",
          gap: theme.spacing(2),
          animation: "scrollY 30s linear infinite",
          "@keyframes scrollY": {
            "0%": { transform: "translateY(0)" },
            "100%": { transform: "translateY(-50%)" },
          },
        }}
      >
        {[...componentTestimonials, ...componentTestimonials].map((tweet, i) => (
          <Card
            key={i}
            sx={{
              minWidth: 280,
              p: 2,
              borderRadius: "12px",
              border: \`1px solid \${theme.palette.divider}\`,
              bgcolor: "background.paper",
            }}
          >
            <Stack spacing={1.5}>
              <Stack direction="row" spacing={1.5} alignItems="center">
                <Avatar
                  src={tweet.avatar}
                  sx={{
                    width: 40,
                    height: 40,
                    border: \`2px solid \${theme.palette.background.paper}\`,
                    boxShadow: theme.shadows[1],
                  }}
                />
                <Box>
                  <Typography variant="subtitle1" fontWeight={600}>
                    {tweet.name}
                  </Typography>
                  <Typography variant="body2" color="text.secondary">
                    {tweet.handle}
                  </Typography>
                </Box>
                <IconButton size="small" sx={{ color: "#1DA1F2", ml: "auto" }}>
                  <RxTwitterLogo />
                </IconButton>
              </Stack>
              <Typography variant="body1" sx={{ lineHeight: 1.5 }}>
                {tweet.text}
              </Typography>
            </Stack>
          </Card>
        ))}
      </Box>
    </Box>
  );
};
`}
/>

---

#### Diagonal Marquee

A diagonal scrolling grid animation using CSS-only transitions.

<CodePreview
  preview={<MarqueeVariants variant="diagonal" />}
  code={`
import React from "react";
import { Box, Card, Stack, Avatar, Typography, IconButton, useTheme } from "@mui/material";
import { RxTwitterLogo } from "react-icons/rx";

const componentTestimonials = [
  {
    name: "Emma Rodriguez",
    handle: "@emma_frontend",
    avatar: "https://images.unsplash.com/photo-1494790108377-be9c29b29330",
    text: "Diagonal grid marquee made easy. ✨",
  },
  // Add more testimonials like these
];

const DiagonalMarquee = () => {
  const theme = useTheme();

  return (
    <Box sx={{
      height: 400,
      position: "relative",
      overflow: "hidden",
    }}>
      <Box
        sx={{
          display: "grid",
          gridTemplateColumns: "repeat(5, 1fr)",
          gap: theme.spacing(4),
          position: "absolute",
          top: "50%",
          left: "50%",
          transform: "rotate(-8deg)",
          animation: "scrollDiagonal 40s linear infinite",
          "@keyframes scrollDiagonal": {
            "0%": { transform: "translate(0, 0) rotate(-8deg)" },
            "100%": { transform: "translate(-50%, -50%) rotate(-8deg)" },
          },
        }}
      >
        {[...componentTestimonials, ...componentTestimonials].map((tweet, i) => (
          <Card
            key={i}
            sx={{
              minWidth: 280,
              p: 2,
              borderRadius: "12px",
              border: \`1px solid \${theme.palette.divider}\`,
              bgcolor: "background.paper",
            }}
          >
            <Stack spacing={1.5}>
              <Stack direction="row" spacing={1.5} alignItems="center">
                <Avatar
                  src={tweet.avatar}
                  sx={{
                    width: 40,
                    height: 40,
                    border: \`2px solid \${theme.palette.background.paper}\`,
                    boxShadow: theme.shadows[1],
                  }}
                />
                <Box>
                  <Typography variant="subtitle1" fontWeight={600}>
                    {tweet.name}
                  </Typography>
                  <Typography variant="body2" color="text.secondary">
                    {tweet.handle}
                  </Typography>
                </Box>
                <IconButton size="small" sx={{ color: "#1DA1F2", ml: "auto" }}>
                  <RxTwitterLogo />
                </IconButton>
              </Stack>
              <Typography variant="body1" sx={{ lineHeight: 1.5 }}>
                {tweet.text}
              </Typography>
            </Stack>
          </Card>
        ))}
      </Box>
    </Box>
  );
};
`}
/>
